# Gradio Space requirements — HF installs these via pip on startup
# torch is installed by unsloth (it picks the right CUDA version automatically)
pyaudioop        # Python 3.13 removed audioop; pydub (gradio dep) needs this
gradio>=4.0
unsloth
numpy
stable-baselines3
gymnasium
huggingface_hub
trl
accelerate
bitsandbytes
xformers
matplotlib
